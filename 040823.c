#include <stdio.h>
int main() {
   // printf() displays the string inside quotation
   //printf("Hello, World!");
   
   int nums[]={9, -1, -5, 7, 2, 11, 5, 1, 3};
   int length = 9;
   int upper=nums[0];
   int lower=0;
   int missing=-1;

   for (int i=0; i<length;i++){

   }
    missing = lower + 1;
   printf("upper: %d, lower: %d, missing: %d\n",upper, lower, missing);
   
   return 0;
}